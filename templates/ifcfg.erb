DEVICE="<%= @name %>"
BOOTPROTO="none"
NM_CONTROLLED="no"
ONBOOT="<%= @onboot %>"
TYPE="InfiniBand"
IPADDR="<%= @ipaddr %>"
NETMASK="<%= @netmask %>"
<% if @gateway -%>
GATEWAY="<%= @gateway %>"
<% end -%>
CONNECTED_MODE="<%= @connected_mode %>"
<% if @mtu -%>
MTU="<%= @mtu %>"
<% end -%>
